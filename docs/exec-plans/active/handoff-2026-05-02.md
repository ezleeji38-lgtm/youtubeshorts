# 세션 핸드오프 — 2026-05-02

> 이 문서는 다음 세션(본인 또는 새 Claude 세션)이 컨텍스트 손실 없이 이어받을 수 있도록 남기는 스냅샷.

## 오늘 한 일 (커밋 단위)

| 커밋 | 항목 | 변경 파일 |
|---|---|---|
| `1f5b4bd` | V0.2 #1 — Creatomate 제거 → 로컬 FFmpeg + ASS 듀얼 자막 합성 | render.py 신규, orchestrator/steps/config/.env.example/README/requirements |
| `01b63ab` | V0.2 #2 — Playlist/Song 데이터 모델 + 7개 CRUD 라우터 | db.py 확장, api/playlists.py + api/songs.py 신규, main.py 라우터 등록, README |
| `ab659fd` | docs — AGENTS.md + 채널 콘셉트 + V0.2 결정 로그 | AGENTS.md, docs/product-specs/{index,channel-concept}.md, docs/exec-plans/active/v0.2.md, README 링크 |

## 현재 V0.2 진행도

- [x] **#1** Creatomate → FFmpeg + ASS
- [x] **#2** Playlist / Song 모델 + CRUD
- [ ] **#3** 곡 단위 일괄 렌더 엔드포인트 (`POST /api/songs/{id}/render`) — 설계 메모는 [v0.2.md](v0.2.md#다음-단계-v02-3-설계-메모)에
- [ ] **#4** 자막 톤앤매너 튜닝

## 미해결 / 의사결정 대기

| 항목 | 상태 | 결정 필요한 것 |
|---|---|---|
| **ffmpeg-full 설치** | 미실행 | macOS 본 컴퓨터의 `ffmpeg` (libass 빠짐) → `ffmpeg-full`로 교체 여부. 실제 렌더 테스트 하려면 필요. |
| **동생 로컬 셋업** | 미진행 | 동생 OS 확인 + 설치 가이드. Windows면 별도 자막 폰트(`Malgun Gothic`) 설정 안내. |
| **교수님 후속 검토** | 일정 미정 | 다음 검토 시점에 보여드릴 데모 (실제 렌더된 mp4 한 편) 준비할지. |
| **`coolvibeply` 잔여 레포** | 미삭제 | <https://github.com/ezleeji38-lgtm/coolvibeply> Danger Zone에서 삭제 가능. 작동 영향 없음. |

## 다음 세션 시작 절차

```bash
cd "/Users/ijiyeon/커서/youtub shorts"

# 1) 현재 상태 확인
git status
git log --oneline -5

# 2) V0.2 결정 로그 복습
cat docs/exec-plans/active/v0.2.md

# 3) 의존성·임포트 정상 확인
PYTHONPATH=. python3 -c "
from backend.main import app
print('routes:', sorted({r.path for r in app.routes if hasattr(r,'path')}))
"

# 4) 서버 띄워보고 싶으면
uvicorn backend.main:app --reload
# → http://localhost:8000/docs
```

## 다음에 할 일 우선순위 후보

1. **V0.2 #3 — 곡 단위 일괄 렌더** (코드 작업, 1-2시간)
    - `POST /api/songs/{song_id}/render`
    - intro/highlight 구간 두 개 → Short 두 개 생성 + 둘 다 BackgroundTasks
    - 각 구간 60초 이내 검증
2. **ffmpeg-full 설치 + 진짜 한 곡 렌더 테스트** (환경 설정, 30분-1시간)
    - 실제로 mp4 한 개가 나오는지 end-to-end 검증
    - libass 자막 위치·크기 1차 확인
3. **V0.2 #4 — 자막 스타일 튜닝** (디자인 작업, 2번 이후)
    - 폰트 크기·색상·외곽선 두께
    - 한글 가독성 테스트
4. **동생 셋업 가이드** (문서 작업, 1시간)
    - Windows·macOS별 설치 단계
    - `.env` 본인 키로 채우는 방법

## 환경·도구 상태

- **Repo**: <https://github.com/ezleeji38-lgtm/youtubeshorts> (public)
- **Local path**: `/Users/ijiyeon/커서/youtub shorts`
- **Git author**: `ezleeji38-lgtm <ezleeji38@gmail.com>` (이 레포에만 로컬 설정)
- **gh CLI**: 인증됨 (`ezleeji38-lgtm`), `delete_repo` 스코프는 없음
- **ffmpeg**: 설치됨이지만 libass 빠짐 → 렌더 실행은 불가
- **API 키**: `.env` 미작성 (실제 파이프라인 돌리려면 OpenAI + Gemini 채워야 함)

## 절대 잊지 말 것 (회귀 방지)

[AGENTS.md](../../../AGENTS.md)에 나오지만 한 번 더:

- ❌ Supabase·JWT·Fernet 사용자 키 암호화 다시 도입
- ❌ Creatomate 등 외부 렌더 API 의존 추가
- ❌ `Short.user_id` 컬럼 부활
- ❌ `frontend/` 폴더 — V1.0까지는 백엔드만
